# Adding Options

Configuration options are intended to be declared in manifests under `manifests/`. Later phases will use these manifests to generate defaults, schema content, and Markdown documentation deterministically.

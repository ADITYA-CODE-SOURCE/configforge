# Architecture

ConfigForge separates configuration loading, validation, compilation, and runtime evaluation. Phase 1 contains the typed configuration model and strict loader. Later phases add immutable runtime policies, middleware, generation, and the example API.
